<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <title>Responsive Profile Card</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
   <link rel="stylesheet" href="../stylesheets/profile.css">
</head>

<body>
    <div class="w-full px-10 py-3 flex justify-between items-center">
        <h3 class="text-2xl font-bold text-gray-800">Pinterest</h3>
        <div class="flex gap-10 items-center">
            <a href="/profile" class="text-gray-700 hover:text-gray-900 transition duration-300">Profile</a>
            <a href="/feed" class="text-gray-700 hover:text-gray-900 transition duration-300">Feed</a>
            <a class="px-3 py-2 bg-red-700 text-white rounded-lg hover:bg-red-600 transition duration-300"
                href="/logout">Logout</a>
        </div>
    </div>
    <div class="profile-card-container flex flex-col">
        <div class="profile-card">
            <div class="profile-image flex justify-center items-center">
                <img
                    src="https://images.unsplash.com/photo-1698866525369-75db28cf88cf?w=500&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHx0b3BpYy1mZWVkfDE3fHRvd0paRnNrcEdnfHxlbnwwfHx8fHw%3D"
                    alt="Profile Picture">
            </div>
            <div class="profile-info">
                <h2 class="name text-xl font-bold text-gray-800"><%= user.fullname %></h2>
                <h3 class="designation text-gray-600">@<%= user.username %></h3>
                <p class="description text-gray-700">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed
                    condimentum arcu a luctus blandit.</p>
            </div>
            <div>
                <a href="/add"
                    class="add-post-button text-white bg-red-700 text-xs font-semibold mt-10">Add New Post</a>
            </div>
        </div>
        <div class="card mt-8">
            <% if (user.posts.length > 0) { %>
                <a href="/show/posts"class="inline-block text-xl font-semibold mt-3 text-gray-800">
                    <div class="w-52 h-40 bg-zinc-200 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover"
                            src="/images/uploads/<%= user.posts[0].image %>" alt="">
                    </div>
                    <div>Your Uploaded Pins</div>
                </a>
            <h5 class="text-sm font-medium opacity-60"><%= user.posts.length %> Pins</h5>
            <% } else { %>
            <p>No uploaded pins yet.</p>
            <a href="/add" class="add-post-button text-white bg-red-700 text-xs font-semibold mt-3">Add New Post</a>
            <% } %>
        </div>
    </div>
    <div class="social-links mt-8">
        <a href="#" class="social-icon"><i class="fab fa-facebook-f"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-twitter"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
    </div>
</body>

</html>
